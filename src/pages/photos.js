import React from 'react';

const Photos = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Photos</h1>
    </div>
  );
};

export default Photos;
